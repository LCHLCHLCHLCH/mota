#pragma once
#include <stdint.h>
#include "game/player.h"

// ============================================================
// 魔法区域伤害：领域（巫师）与夹击（魔法警卫）
// - 领域：初级巫师(126) 100 / 高级巫师(127) 200，进入四向相邻格触发，可叠加
// - 夹击：魔法警卫(131)，进入"两个同种守卫中间隔一格"的中间格触发，
//         伤害 = floor((当前血量 - 领域伤害) / 2)（动态）
// - 神圣盾免疫；血量不足以承受则阻止行动
// ============================================================

// 重新计算当前楼层的区域伤害表（进入楼层/击杀怪物后调用）
void recomputeZoneDamage(uint8_t floor);

// 踏入目标格前的区域伤害判定：返回 false 表示被阻止（已给出提示，未扣血）；
// 返回 true 表示允许（若该格有区域伤害且未免疫，已扣除）
bool checkZoneEntry(Player& player, uint8_t floor, uint8_t x, uint8_t y, uint8_t tile);
